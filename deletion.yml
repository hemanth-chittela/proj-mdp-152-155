---
 - name: deleting the cluster
   hosts: g1
   become: yes

  

   tasks:

   - name: Deleting cluster 
     shell: kops delete cluster --name=project10.k8s.local --state=s3://nw-prj --yes
     
     
     
...

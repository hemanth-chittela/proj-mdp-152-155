---
- name: Targeting the host
  hosts: g1
  become: yes

  tasks:
   - name: unzip
     yum:
       name: unzip
       state: present

  tasks:
   - name: Install AWS CLI
     package:
       name: awscli
       state: present

  tasks:
  - name: install pip
    package: 
      name: python2-pip
      

  tasks:
  - name: install kops directly
    pip: 
       name: kops
       state: present
         


...
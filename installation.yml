---
- name: Targeting the host
  hosts: g1
  become: yes

  tasks:
   - name: unzip
     yum:
       name: unzip
       state: present

  
   - name: Install AWS CLI'
     package:
       name: awscli
       state: present

  
  - name: install pip
    yum: 
      name: python2-pip
      state: present
      

  
  - name: install kops directly
    pip: 
       name: kops
       state: present
         


...